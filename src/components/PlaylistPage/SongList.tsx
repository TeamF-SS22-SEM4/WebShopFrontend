import {useState} from "react";


function SongList() {
    let [selectedIndex, setSelectedIndex] = useState(0);

    return (
        <>
            list
        </>
    )
}

export default SongList;