# Variables in .env-File with example Values

OUTWARD_PORT=80 # The port on which the website should be reachable. Defaults to 80 if unset

# NOTE on variables that are accesses in the react app: need to start with REACT_APP, else will be undefined

# Where to reach the rest api
REACT_APP_API_HOST=localhost
REACT_APP_API_PORT=8080
